schema: spec-driven

context: |
  Tech stack: Java 17, Gradle 8.x, Serenity BDD 4.2.9 + Screenplay REST + Cucumber 7.18.1 + REST Assured 5.4.0 + Jackson 2.17.2
  Architecture: Gradle multi-module (root + app), Screenplay pattern (Tasks/Questions/Actor memory), package com.automation
  Testing: Serenity BDD + Cucumber 7 + JUnit 4 runner (CucumberWithSerenity)
  Style: Default Gradle/Java conventions, no dedicated linter/formatter

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbering (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
    - Load relevant coding skills for the project stack
  verify:
    - Run tests if test infrastructure exists
    - Compare implementation against every spec scenario
  archive:
    - Warn before merging destructive deltas (large removals)
